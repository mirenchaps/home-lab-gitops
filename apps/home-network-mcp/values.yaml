nameOverride: home-network-mcp

replicaCount: 4

image:
  tag: sha-6c3bd83

containerPort: 8000

service:
  nodePort: 30080

# Probes only -- this app's device metrics already go to Grafana Cloud via
# Alloy, which stays untouched. Not scraped locally unless asked for.
healthCheck:
  path: /metrics
