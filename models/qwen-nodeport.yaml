apiVersion: v1
kind: Service
metadata:
  name: qwen-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app: isvc.qwen-predictor
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30082