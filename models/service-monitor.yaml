apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: servicemonitor
  namespace: default
  labels: 
    release: prometheus
spec:
  selector:
    matchLabels:
      component: predictor
  endpoints:
    - port: qwen-predictor
      path: /metrics
      interval: 15s